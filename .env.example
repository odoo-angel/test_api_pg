# Server
PORT=3000

# Database (local / docker-compose.local.yml)
# DB_HOST=34.60.76.101

# Database (Cloud Run + Cloud SQL)
# DB_HOST=/cloudsql/etl-creekside-park:us-central1:report-system-25

DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=creekside_db
DB_SSL=false   

# Sessions (for Google OAuth)
SESSION_SECRET=change-this-session-secret

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT
JWT_SECRET=change-this-jwt-secret

# Default Admin User (ONLY FOR FIRST RUN)
DEFAULT_ADMIN_EMAIL=admin@creekside.com
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_FIRST_NAME=Admin
DEFAULT_ADMIN_LAST_NAME=User